spring:
  cloud:
    kubernetes:
      enabled: false
      discovery.enabled: false
  liquibase:
    enabled: true
  jpa:
    hibernate:
      ddl-auto: none
  data:
    jpa:
      repositories:
        bootstrap_mode: default


logging:
  level:
    org.springframework.jdbc.core.JdbcTemplate: DEBUG